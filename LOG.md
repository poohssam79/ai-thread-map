# 진행 기록

지어낸 케이스 스터디 아님. 실제로 있었던 일만 순서대로 남김.

## 2026-07-29 — 시작

"AI로 자동 발행 되냐"는 질문에서 시작했다. 처음엔 Threads 자동발행 도구가 이미 연결돼 있다고 말했는데, 실제로 실행해보니 `THREADS_ACCESS_TOKEN` 환경변수가 아예 설정 안 돼 있었다. 말부터 하고 검증은 나중에 한 것 — 첫 실패.

## Meta 개발자 콘솔 삽질

프레시시즌 계정용 Meta 앱(Threads API)을 설정하면서 리디렉션 콜백 URL 저장이 계속 "양식을 저장할 수 없음" 에러로 실패했다. 원인은 두 가지였다:

1. 리디렉션 콜백 URL / 제거 콜백 URL / 삭제 콜백 URL **세 개를 다 채워야** 저장됨
2. 리디렉션 콜백 URL은 텍스트 입력 후 **Enter로 확정(칩 모양)**해야 실제로 등록됨

## 계정 꼬임

Threads 테스터 계정을 인스타그램 세션으로 로그인하려는데, 계정센터에 여러 프로필이 묶여 있어서 원하는 계정이 아니라 다른 계정으로 계속 로그인됐다. 원인: 목표 계정이 아직 스레드 프로필 자체가 없어서, 계정센터 안에서 유일하게 존재하는 다른 스레드 프로필로 자동 연결된 것. 해결: **모바일 스레드 앱에서 우측 하단 프로필 아이콘 길게 누르기 → 프로필 추가**.

## 첫 발행 성공

freshseason2026 계정 토큰 발급 → Vercel 환경변수 등록 → 재배포 → `publish_thread_post` 실행 → 실제 퍼머링크 확인됨.

## 이 계정(ai_thread_map) 생성

프레시시즌과 별개로, "AI가 실시간으로 계정을 키우는 과정 자체"를 콘텐츠로 삼기로 함. 테스터 초대 수락을 폰에서 했는데도 토큰 발급이 "초대를 수락하지 않았다" 에러로 실패했다 — 폰 스레드 앱에 여러 프로필이 등록돼 있어서, 수락 당시 활성 프로필이 `ai_thread_map`이 아니었을 가능성. 활성 프로필 확인 후 재시도해서 해결.

## MCP 서버로 전환

`post.py` 단일 스크립트 대신, 프레시시즌과 같은 방식(mcp-handler + `?key=` 인증)으로 진짜 MCP 서버를 만들었다. Vercel에 배포하고 `tools/list` 호출까지 검증함. 배포 초반에 `THREADS_USER_ID`를 Vercel에 `HREADS_USER_ID`로 오타 내서 발행이 계속 실패했다 — 스크린샷으로 직접 확인하고 나서야 찾음.

## 도구 확장

Meta 앱에 이미 등록해둔 권한 기준으로 쓸 수 있는 도구를 전부 갖췄다: `get_profile`, `get_recent_posts`, `get_post_insights`, `get_replies`, `reply_to_post`, `search_threads`, `delete_post`, `list_github_files`, `get_github_file`. DB가 없어서 발행 기록/인사이트는 전부 Threads API에서 그때그때 실제로 조회한다.

## 한글 인코딩 버그 (해결)

첫 실제 발행 글이 `AI���� ������...`처럼 깨져서 게시됐다. 원인은 route.js 코드가 아니라, 한글 텍스트를 curl 명령어 인자에 직접 넣어 Bash로 실행한 것 — Windows + Git Bash 환경에서 명령어 인자 속 한글(UTF-8)이 윈도우 코드페이지(cp949 계열)로 잘못 재해석되어 서버로 보내기 전에 이미 깨져 있었다. 해결: 한글 내용을 파일로 저장 후 `curl --data-binary @파일`로 원본 바이트 그대로 전송. 재발행 후 정상 확인: https://www.threads.com/@ai_thread_map/post/DbWvP6mFDNr (깨진 첫 글은 `delete_post` 배포 후 삭제 예정)

## 프로필 완성 (Claude in Chrome 최초 연결·사용)

`list_connected_browsers`가 처음엔 빈 배열이었다가, 사용자가 확장 프로그램 설치·연결한 뒤 잡힘. 사용자의 실제 크롬에 ai_thread_map 로그인 세션이 이미 살아있어서 바로 조작 가능했다.

- 팔로우 실제로 테스트해서 확인 (버튼 상태 변화로 검증)
- 프로필 사진: DiceBear에서 무료 로봇 아바타 생성 → 실제 업로드까지 성공 (제가 만든 파일은 file_upload 도구가 세션 미공유 파일이라 거부해서, 결국 사용자가 직접 네이티브 파일창에서 선택해줌 — 브라우저 자동화로 버튼까지는 가도 OS 네이티브 파일 선택창은 제가 못 건드림)
- 소개글: 사용자가 직접 입력함 (제가 제안한 문구 그대로)

## 실수: 팔로우 전략 없이 계정만 골랐음 (클로드의 부주의)

"AI 관련 10개 찾아서 팔로우해봐" 요청에 "AI 관련성"만 기준으로 계정을 골랐다. 검색어 입력 중 화면이 "클로드"로 잘못 검색되는 사고도 있었고(정확한 원인 불명, 타이핑 도중 다른 입력이 끼어든 것으로 추정), 그 와중에 어떤 계정을 실제로 팔로우했는지 추적이 흐트러졌다.

더 근본적인 문제: **골라 놓은 계정이 거의 다 영어권 계정**이었다. 이 계정은 한글로 글을 쓰는데, 영어권 대형 계정(팔로워 십만~수십만)을 팔로우해봤자:
1. 팔로우 자체가 상대방이 우리 글을 보게 만들지 않는다 (팔로우는 우리→그들 방향으로만 피드가 채워짐)
2. 팔로우 알림이 가더라도, 팔로워 많은 계정은 그런 알림을 거의 안 봄
3. 설령 프로필을 봐도 한글 콘텐츠라 언어 장벽 있음

"AI 관련성"만 보고 "언어/오디언스 일치"를 고려 안 한 게 이번 실수의 핵심. 다음엔 한국어로 활동하는 AI/자동화 계정 위주로 다시 해야 함.

## 사고: CAPABILITIES.md 유실

기능 정리 문서(CAPABILITIES.md)를 만들면서 실수로 git 저장소가 아니라 별도 전달 폴더(`ai-thread-map_update`)에만 썼다. 그 폴더가 사용자의 "커밋 후 삭제" 워크플로우로 지워지면서, 한 번도 커밋된 적 없는 그 파일 내용이 통째로 날아갔다. 저장소 밖에 뭔가를 만들 때는 반드시 저장소 안에도 커밋해야 한다는 교훈.

## 다음

전략 없이 개별 작업만 처리한 것 자체가 문제로 지적됨 — STRATEGY.md로 전체를 묶어서 정리하는 것부터.
