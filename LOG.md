# 진행 기록

지어낸 케이스 스터디 아님. 실제로 있었던 일만 순서대로 남김.

## 2026-07-29 — 시작

"AI로 자동 발행 되냐"는 질문에서 시작했다. 처음엔 Threads 자동발행 도구가 이미 연결돼 있다고 말했는데, 실제로 실행해보니 `THREADS_ACCESS_TOKEN` 환경변수가 아예 설정 안 돼 있었다. 말부터 하고 검증은 나중에 한 것 — 첫 실패.

## Meta 개발자 콘솔 삽질

프레시시즌 계정용 Meta 앱(Threads API)을 설정하면서 리디렉션 콜백 URL 저장이 계속 "양식을 저장할 수 없음" 에러로 실패했다. 원인은 두 가지였다:

1. 리디렉션 콜백 URL / 제거 콜백 URL / 삭제 콜백 URL **세 개를 다 채워야** 저장됨
2. 리디렉션 콜백 URL은 텍스트 입력 후 **Enter로 확정(칩 모양)**해야 실제로 등록됨 — 그냥 타이핑만 하면 저장 시점에 "지정 안 됨" 에러가 남

## 계정 꼬임

Threads 테스터 계정을 인스타그램 세션으로 로그인하려는데, 계정센터에 여러 프로필(개인 계정 포함)이 묶여 있어서 원하는 계정이 아니라 다른 계정으로 계속 로그인됐다. 원인: 목표 계정이 아직 스레드 프로필 자체가 없어서, 계정센터 안에서 유일하게 존재하는 다른 스레드 프로필로 자동 연결된 것.

해결: **모바일 스레드 앱에서 우측 하단 프로필 아이콘 길게 누르기 → 프로필 추가** — 이게 새 인스타 계정에 스레드 프로필을 만드는 공식 경로. 웹에서는 이 흐름이 명확하게 안 보인다.

## 첫 발행 성공

freshseason2026 계정 토큰 발급 → Vercel 환경변수 등록 → 재배포 → `publish_thread_post` 실행 → 실제 퍼머링크 확인됨. 여기까지 사람이 직접 확인 가능한 증거로 검증됨.

## 시장 조사

"뭐가 터지는 글이냐"는 질문에 처음엔 계정 2~3개만 보고 성급하게 패턴을 말했다가 지적받았다. 다시 40개 이상 실제 게시물의 좋아요/리포스트 숫자를 직접 확인:

- 순수 정보나열형: 좋아요 5~90대
- 총정리/치트시트형, 위트있는 한 줄, 개인서사+구체적 수치: 좋아요 800~3,600+

## 이 계정(ai_thread_map) 생성

프레시시즌과 별개로, "AI가 실시간으로 계정을 키우는 과정 자체"를 콘텐츠로 삼기로 함. 새 인스타 계정 생성 → 같은 Meta 앱 설정 과정을 반복했는데, 이번엔 뭐가 문제였는지 알아서 리디렉션 URL 저장은 빠르게 끝남.

테스터 초대 수락을 폰에서 했는데도 토큰 발급이 "초대를 수락하지 않았다" 에러로 실패했다 — 폰 스레드 앱에 여러 프로필이 등록돼 있어서, 수락 당시 활성 프로필이 `ai_thread_map`이 아니었을 가능성. 활성 프로필 확인 후 재시도해서 해결.

## MCP 서버로 전환

`post.py` 단일 스크립트 대신, 프레시시즌과 같은 방식(mcp-handler + `?key=` 인증)으로 진짜 MCP 서버를 만들었다. Vercel에 배포하고 실제로 `tools/list` 호출까지 검증함.

배포 초반에 `THREADS_USER_ID`를 Vercel에 `HREADS_USER_ID`로 오타 내서 발행이 계속 실패했다 — 맨 앞 글자 하나가 빠진 것. 스크린샷으로 직접 확인하고 나서야 찾음.

## 도구 확장

한 번에 필요한 것만 찔끔찔끔 추가하지 말고, Meta 앱에 이미 등록해둔 권한(답글 관리/읽기, 인사이트, 키워드 검색) 기준으로 쓸 수 있는 도구를 전부 갖췄다: `get_profile`, `get_recent_posts`, `get_post_insights`, `get_replies`, `reply_to_post`, `search_threads`, `list_github_files`, `get_github_file`.

DB가 없어서 발행 기록/인사이트는 전부 Threads API에서 그때그때 실제로 조회한다 — 추측하지 않는다는 원칙을 도구 레벨에서 강제한 것.

## 다음

이 MCP 서버로 실제 첫 글을 발행하고, `get_recent_posts`/`get_post_insights`로 결과를 실제 숫자로 확인하는 것부터.
