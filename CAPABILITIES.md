# 클로드가 뭘 할 수 있고 뭘 못 하는지 (최종 정리)

이 프로젝트 하면서 실제로 확인된 것만 적는다. 추측 없음. 두 가지 경로로 나눠서 정리한다 — **API 경로**(MCP 서버, 대화 없이도 코드로 동작)와 **브라우저 자동화 경로**(Claude in Chrome, 사람처럼 화면을 직접 조작, 대화가 열려있어야 동작).

## 오늘 있었던 버그: 한글 깨짐 (해결됨)

**증상**: `publish_post`로 올린 첫 글이 스레드에 `AI���� ������...`처럼 깨져서 게시됨.
**원인**: 코드 문제가 아니라, 한글 텍스트를 curl 명령어 인자에 직접 넣어 Bash로 실행한 게 문제였다. Windows + Git Bash 환경에서 명령어 인자 속 한글(UTF-8)이 윈도우 코드페이지(cp949 계열) 처리 과정에서 잘못 재해석되어, 서버로 보내기 전에 이미 깨져 있었다.
**해결**: 한글 내용을 파일로 먼저 저장(Write 도구는 UTF-8을 정확히 씀) → `curl --data-binary @파일`로 원본 바이트 그대로 전송. 실제로 재발행해서 정상 확인됨: https://www.threads.com/@ai_thread_map/post/DbWvP6mFDNr
**교훈**: 이 환경에서 한글(비ASCII) 텍스트를 API에 보낼 때는 항상 파일 경유.

## API 경로 (MCP 서버, `app/api/mcp/route.js`)

실제 배포되어 검증된 도구 10개 + 오늘 밤 추가한 `delete_post`:

| 도구 | 상태 |
|---|---|
| publish_post | ✅ 실제 발행 검증됨 |
| refresh_threads_token | 코드는 있음, 실행 테스트는 안 함 |
| get_profile | ✅ 실제 조회 검증됨 |
| get_recent_posts | ✅ 실제 조회 검증됨 (permalink 버그도 이걸로 찾음) |
| get_post_insights | 코드는 있음, 실행 테스트는 안 함 |
| get_replies / reply_to_post | 코드는 있음, 실행 테스트는 안 함 |
| search_threads | 코드는 있음, 실행 테스트는 안 함 |
| delete_post | 코드는 있음, threads_delete 권한 미등록이라 아직 안 됨 |
| list_github_files / get_github_file | 코드는 있음(Fresh Season에서는 검증된 패턴), 이 저장소 대상 테스트는 안 함 |

**API로 안 되는 것 (공식 확인)**: 프로필 사진/소개글/이름 수정, 팔로우/언팔로우 — Threads 공식 API에 해당 엔드포인트가 아예 없음.

## 브라우저 자동화 경로 (Claude in Chrome, 오늘 밤 처음 연결·테스트함)

`list_connected_browsers`가 처음엔 빈 배열이었다가, 사용자님이 확장 프로그램 설치·연결하신 뒤 잡힘. 사용자님의 실제 크롬에 ai_thread_map 로그인 세션이 이미 살아있어서 바로 조작 가능했다.

**실제로 테스트해서 확인된 것:**
- ✅ **팔로우** — 팔로우 추천에 뜬 계정(pcos.wellnesscysterhood)을 실제로 클릭해서 팔로우함. 버튼이 "팔로우"→"팔로잉"으로 바뀌는 것까지 확인.
- ✅ **프로필 편집 화면 접근** — "프로필 편집" 클릭하면 이름/사용자이름/소개/관심사/링크/공개범위 다 수정 가능한 모달이 열림. 실제 값 변경까지는 안 해봤지만 필드 접근·타이핑은 가능한 상태 확인.
- **프로필 사진 업로드** — 버튼은 확인했지만 업로드할 이미지 파일이 없어서 실제 업로드까지는 테스트 안 함. 파일만 있으면 될 것으로 보임(미검증).

**API로는 안 되지만 브라우저 경로로는 되는 것**: 팔로우/언팔로우, 프로필 사진/소개/이름 수정 — 전부 이 경로로 가능.

**브라우저 경로의 근본적 제약**: 이건 지금 이 대화가 열려있는 동안만 동작한다. 사람이 "해줘"라고 말해야 실행되고, 대화 밖에서 저절로 도는 자동화는 아니다. API 경로처럼 서버에 심어서 스케줄러로 24시간 돌리는 건 안 됨 (브라우저를 계속 열어놓고 클로드가 상시 대기해야 하는데, 그런 구조는 없음).

## 완전 자동화(사람 개입 없이 계속 돌아가게)하려면 필요한 것

1. **스케줄링** — Vercel Cron Job 등으로 `publish_post`를 주기적으로 자동 호출. 브라우저 경로(팔로우, 프로필 편집)는 이 방식으로 자동화 불가 — API가 없는 이상 사람이 대화로 시켜야 함
2. **상태 저장(DB)** — 지금은 Supabase가 없어서 "이미 답글 단 댓글"을 기억 못 함. 자동 답글 돌리려면 최소 상태 테이블 필요
3. **Vercel CLI 토큰** — 지금은 사람이 매번 "배포" 버튼 눌러야 함
4. **GitHub 저장소 접근 권한 정리** — push 권한 있는 계정으로 통일 (지금은 minsiljang0 계정으로 로그인돼 있어서 poohssam79 소유 저장소에 push 거부됨)
5. **콘텐츠 승인 자동화 여부 결정** — 지금은 "발행 전 사람 승인" 원칙. 없애려면 CONTENT_GUIDE.md 체크리스트만으로 대체 가능한지 검증 필요

## 결론

**"Claude가 어디까지 되냐"의 정답은 하나가 아니다**:
- 서버에 심어서 24시간 자동으로 돌리고 싶은 것(글 발행, 댓글 응답, 인사이트 확인) → API 경로, 이미 구축됨
- 사람이 "이거 해줘" 할 때마다 화면 조작으로 처리하는 것(팔로우, 프로필 편집) → 브라우저 경로, 오늘 밤 처음 연결해서 검증함
- 이 두 경로가 커버 못 하는 것(계정 생성, 로그인, 결제 등) → 사람이 직접
