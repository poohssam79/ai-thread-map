# 클로드가 뭘 할 수 있고 뭘 못 하는지 (최종 정리)

이 프로젝트 하면서 실제로 확인된 것만 적는다. 추측 없음. 두 가지 경로로 나눠서 정리한다 — **API 경로**(MCP 서버, 대화 없이도 코드로 동작)와 **브라우저 자동화 경로**(Claude in Chrome, 사람처럼 화면을 직접 조작, 대화가 열려있어야 동작).

## 오늘 있었던 버그: 한글 깨짐 (해결됨)

**증상**: `publish_post`로 올린 첫 글이 스레드에 `AI���� ������...`처럼 깨져서 게시됨.
**원인**: 코드 문제가 아니라, 한글 텍스트를 curl 명령어 인자에 직접 넣어 Bash로 실행한 게 문제였다. Windows + Git Bash 환경에서 명령어 인자 속 한글(UTF-8)이 윈도우 코드페이지(cp949 계열) 처리 과정에서 잘못 재해석되어, 서버로 보내기 전에 이미 바이트가 깨져 있었다.
**해결**: 한글 내용을 먼저 파일로 저장(Write 도구는 UTF-8을 정확히 씀) → `curl --data-binary @파일`로 그 파일의 원본 바이트를 그대로 전송. 실제로 재발행해서 정상 확인됨: https://www.threads.com/@ai_thread_map/post/DbWvP6mFDNr
**교훈**: 이 환경에서 한글(비ASCII) 텍스트를 API에 보낼 때는 항상 파일 경유.

## API 경로 (MCP 서버, `app/api/mcp/route.js`)

| 도구 | 상태 |
|---|---|
| publish_post | ✅ 실제 발행 검증됨 |
| refresh_threads_token | 코드는 있음, 실행 테스트는 안 함 |
| get_profile | ✅ 실제 조회 검증됨 |
| get_recent_posts | ✅ 실제 조회 검증됨 (permalink 버그도 이걸로 찾음) |
| get_post_insights | ✅ 실제 조회 검증됨 (조회수·좋아요 등 실제 숫자 확인) |
| get_replies / reply_to_post | 코드는 있음, 실행 테스트는 안 함 |
| search_threads | 코드는 있음, 실행 테스트는 안 함 |
| delete_post | 코드는 있음, threads_delete 권한 미등록이라 아직 안 됨 |
| list_github_files / get_github_file | 코드는 있음(Fresh Season에서는 검증된 패턴), 이 저장소 대상 테스트는 안 함 |

**API로 안 되는 것 (공식 확인)**: 프로필 사진/소개글/이름 수정, 팔로우/언팔로우 — Threads 공식 API에 해당 엔드포인트가 아예 없음.

## 브라우저 자동화 경로 (Claude in Chrome, 오늘 밤 처음 연결·테스트함)

`list_connected_browsers`가 처음엔 빈 배열이었다가, 확장 프로그램 설치·연결 후 잡힘. 사용자의 실제 크롬에 ai_thread_map 로그인 세션이 이미 살아있어서 바로 조작 가능했다. 세션 도중 연결이 한 번 끊겼다가 재연결됨(원인 불명, 재시도로 해결).

**실제로 테스트해서 확인된 것:**
- ✅ **팔로우** — 여러 계정을 실제로 클릭해서 팔로우함. 버튼이 "팔로우"→"팔로잉"으로 바뀌는 것까지 확인. (단, 그리드형 프로필 카드는 "+" 클릭 시 바로 팔로우되는 게 아니라 미리보기 카드가 뜨고, 그 안의 "팔로우" 버튼을 다시 눌러야 함 — 처음엔 이 구조를 몰라서 여러 번 헛클릭함)
- ✅ **프로필 사진 업로드** — 실제로 성공함. 단, 제가 만든 파일은 file_upload 도구가 "사용자가 세션에 공유한 파일만 허용"이라며 거부함 — 제가 Bash로 만든 파일은 경로를 알아도 업로드 불가. 결국 브라우저가 연 네이티브 OS 파일 선택창(제가 볼 수도 조작할 수도 없는 영역)에서 사용자가 직접 파일을 선택해줘서 완료됨.
- ✅ **프로필 편집 화면 접근** — 이름/사용자이름/소개/관심사/링크/공개범위 수정 가능한 모달 열림 확인.

**API로는 안 되지만 브라우저 경로로는 되는 것**: 팔로우/언팔로우, 프로필 사진/소개/이름 수정 — 전부 이 경로로 가능.

**브라우저 경로의 근본적 제약**: 이건 지금 대화가 열려있는 동안만 동작한다. 사람이 "해줘"라고 말해야 실행되고, 대화 밖에서 저절로 도는 자동화는 아니다.

**진짜 안 되는 것 — 네이티브 OS 다이얼로그**: 파일 선택창처럼 브라우저 밖(운영체제 레벨)에서 뜨는 UI는 Claude in Chrome으로도 절대 못 봄. 여기서 막히면 사용자가 직접 조작해야 함.

## 완전 자동화(사람 개입 없이 계속 돌아가게)하려면 필요한 것

1. **스케줄링** — Vercel Cron Job 등으로 `publish_post`를 주기적으로 자동 호출. 브라우저 경로(팔로우, 프로필 편집)는 이 방식으로 자동화 불가
2. **상태 저장(DB)** — 지금은 Supabase가 없어서 "이미 답글 단 댓글"을 기억 못 함
3. **Vercel CLI 토큰** — 지금은 사람이 매번 "배포" 버튼 눌러야 함
4. **GitHub 저장소 접근 권한 정리** — 이 컴퓨터의 git 로그인(minsiljang0)과 저장소 소유 계정(poohssam79)이 달라서 push 거부됨. 지금은 웹에서 파일 직접 업로드로 우회 중
5. **콘텐츠 승인 자동화 여부 결정** — 지금은 "발행 전 사람 승인" 원칙

프로필 사진/소개글, 팔로우는 API 자체가 없어서 브라우저 자동화 경로로만 가능하고, 그마저도 대화가 열려있을 때만 동작함 — 완전 무인 자동화는 안 됨.

## 결론

**"Claude가 어디까지 되냐"의 정답은 하나가 아니다**:
- 서버에 심어서 24시간 자동으로 돌리고 싶은 것(글 발행, 인사이트 확인) → API 경로, 이미 구축됨
- 사람이 "이거 해줘" 할 때마다 화면 조작으로 처리하는 것(팔로우, 프로필 편집) → 브라우저 경로, 오늘 밤 처음 연결해서 검증함
- 이 두 경로가 커버 못 하는 것(계정 생성, 로그인, 네이티브 파일 선택, 결제 등) → 사람이 직접
